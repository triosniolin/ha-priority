pytest-homeassistant-custom-component

# The description tests also need the requirements of every base component that
# `async_get_all_descriptions` imports. Those are derived from the installed
# core instead of being listed here, so that a new core dependency cannot turn
# into a red build on a release that changed nothing in this integration:
#
#   python tests/shim_requirements.py | xargs -r pip install
